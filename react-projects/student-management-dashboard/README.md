<input
type="text"
placeholder="Search students..."
value={searchTerm}
onChange={(e) => setSearchTerm(e.target.value)}
// value + onChange
// value={searchTerm}
// onChange={(e) => setSearchTerm(e.target.value)}

// Purpose: Connect the input box to React state.
/>
